{
    "version": 3,
    "configurePresets": [
        {
            "name": "windows-msvc",
            "displayName": "Windows MSVC",
            "description": "Standalone engine build for Windows with MSVC (no vcpkg, no Qt, no SDL)",
            "generator": "Visual Studio 17 2022",
            "binaryDir": "${sourceDir}/build-windows",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        },
        {
            "name": "default",
            "displayName": "Default (Linux/Unix)",
            "description": "Standalone engine build on Linux",
            "binaryDir": "${sourceDir}/build",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        }
    ]
}
